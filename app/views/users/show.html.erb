<!-- !!!!! This page is not finished and until now just used to se backend output !!!!! -->
<div class="container-user-show">
  <div class="user-data">
    <h1>Hello <%= current_user.first_name %>, how are you today? </h1>
    <p> Registered in <%= current_user.created_at.year %> </p>
  </div>

  <div class="body-user-show">
    <div class="as-seeker">
      <h2>My borrows</h2>

    </div>

    <div class="as-lender">
      <div class="requests-lender">
        <h2>My lendings</h2>

      </div>

      <div class="offers">
        <h2>My loan offers</h3>

      </div>
    </div>
  </div>
</div>





















<div class="container">
  <!-- all user's offers (lender)-->
  <div>

  </div>

  <!-- all user's  loans (as lender) -->
  <div>
    <div class="pending-offers">
      <h2>Your pending offers</h2>
      <% current_user.offers.each do |offer| %>
        <% offer.loans.each do |loan| %>
          <% if loan.status == "pending" %>
            <h3><%= loan.user.first_name %> asks you for a loan</h3>
            <div class="loan-info">
              Requester: <%= loan.user.first_name %> <%= loan.user.last_name %> </br>
              Loan amount: <%= offer.amount %></br>
              From: <%= loan.start_date %></br>
              Until: <%= loan.end_date %></br>
              Total price: <%= loan.total_price %>
            </div>
            <%= link_to "Accept", accepted_by_lender_loan_path(loan) %>
            <%= link_to "Decline", declined_by_lender_loan_path(loan) %>
          <% end %>
        <% end %>
      <% end %>
    </div>

    <div class="pending-offers">
      <h2>Your accepted offers</h2>
      <% current_user.offers.each do |offer| %>
        <% offer.loans.each do |loan| %>
          <% if loan.status == "accepted" %>
            <h3><%= loan.user.first_name %> asks you for a loan</h3>
            <div class="loan-info">
              Requester: <%= loan.user.first_name %> <%= loan.user.last_name %> </br>
              Loan amount: <%= offer.amount %></br>
              From: <%= loan.start_date %></br>
              Until: <%= loan.end_date %></br>
              Total price: <%= loan.total_price %>
            </div>
          <% end %>
        <% end %>
      <% end %>
    </div>

    <div class="pending-offers">
      <h2>Your declined offers</h2>
      <% current_user.offers.each do |offer| %>
        <% offer.loans.each do |loan| %>
          <% if loan.status == "declined" %>
            <h3><%= loan.user.first_name %> asks you for a loan</h3>
            <div class="loan-info">
              Requester: <%= loan.user.first_name %> <%= loan.user.last_name %> </br>
              Loan amount: <%= offer.amount %></br>
              From: <%= loan.start_date %></br>
              Until: <%= loan.end_date %></br>
              Total price: <%= loan.total_price %>
            </div>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>

  <!-- all user's loans (as seeker) -->
  <div>

  </div>
</div>
